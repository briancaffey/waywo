[project]
name = "narration"
version = "0.1.0"
description = "Voice narration generator using TTS with voice cloning"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv]
package = false
